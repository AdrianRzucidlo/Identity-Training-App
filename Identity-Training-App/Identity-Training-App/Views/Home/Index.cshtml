@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    @if(User.Identity.IsAuthenticated)
    {
        <a asp-action="EnableAuthenticator" asp-controller="Account">Set up two factor authenticator.</a>
        <br />
    }
</div>
